import fs from "fs"; import path from "path"; import matter from "gray-matter";
export type Product={slug:string;title:string;image:string;images:string[];category:string;subcategory:string;description:string;highlights:string[];affiliateUrl:string;publishedAt:string;seoTitle?:string;seoDescription?:string;status?:string};
const dir=path.join(process.cwd(),"content","products");
function secureUrl(v:unknown){if(typeof v!=="string")return "";try{const u=new URL(v);return u.protocol==="https:"?u.toString():""}catch{return ""}}
export function getProducts():Product[]{if(!fs.existsSync(dir))return [];return fs.readdirSync(dir).filter(x=>x.endsWith(".md")).map(f=>{const raw=fs.readFileSync(path.join(dir,f),"utf8"),m=matter(raw),d=m.data as Record<string,unknown>;const imgs=Array.isArray(d.images)?d.images.filter((x):x is string=>typeof x==="string").slice(0,5):[];return{slug:f.replace(/\.md$/,""),title:typeof d.title==="string"?d.title:f.replace(/\.md$/,""),image:typeof d.image==="string"?d.image:imgs[0]||"",images:imgs,category:typeof d.category==="string"?d.category:"Gifts & More",subcategory:typeof d.subcategory==="string"?d.subcategory:"",description:m.content.trim(),highlights:Array.isArray(d.highlights)?d.highlights.filter((x):x is string=>typeof x==="string").slice(0,6):[],affiliateUrl:secureUrl(d.affiliateUrl),publishedAt:typeof d.publishedAt==="string"?d.publishedAt:"",seoTitle:typeof d.seoTitle==="string"?d.seoTitle:undefined,seoDescription:typeof d.seoDescription==="string"?d.seoDescription:undefined,status:typeof d.status==="string"?d.status:"published"}}).filter(p=>p.status!=="draft"&&p.affiliateUrl)}
export function getProduct(slug:string){return getProducts().find(p=>p.slug===slug)}
export function getCategories(){const m=new Map<string,Set<string>>();for(const p of getProducts()){if(!m.has(p.category))m.set(p.category,new Set());if(p.subcategory)m.get(p.category)!.add(p.subcategory)}return Array.from(m.entries()).map(([name,s])=>({name,subcategories:Array.from(s).sort()})).sort((a,b)=>a.name.localeCompare(b.name))}
