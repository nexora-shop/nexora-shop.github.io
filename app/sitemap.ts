import type {MetadataRoute} from "next"; import {getProducts} from "../lib/catalog"; export default function sitemap():MetadataRoute.Sitemap{const b=process.env.NEXT_PUBLIC_SITE_URL||"https://nexora1998.netlify.app";const pages=["","/categories/","/about/","/contact/","/affiliate-disclosure/","/privacy/","/terms/","/disclaimer/"];return [...pages,...getProducts().map(p=>`/product/${p.slug}/`)].map(u=>({url:b+u,changeFrequency:"weekly" as const,priority:u===""?1:.7}))}
