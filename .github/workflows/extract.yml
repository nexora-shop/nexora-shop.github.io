name: Extract Nexora Mobile Polished ZIP

on:
  workflow_dispatch:
  push:
    paths:
      - "nexora-mobile-polished1.zip"

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract ZIP
        run: |
          mkdir -p /tmp/nexora
          unzip -o nexora-mobile-polished.zip -d /tmp/nexora

          cp -r /tmp/nexora/* ./

          if [ -d "/tmp/nexora/nexora-mobile-polished" ]; then
            cp -r /tmp/nexora/nexora-mobile-polished/* ./
          fi

      - name: Remove ZIP
        run: rm -f nexora-mobile-polished.zip

      - name: Commit extracted files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --cached --quiet || git commit -m "Extract Nexora mobile polished project"
          git push
