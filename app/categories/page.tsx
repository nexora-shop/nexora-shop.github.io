import Link from "next/link"; import {Header,Footer} from "../components"; import {getCategories} from "../../lib/catalog";
export default function CategoriesPage(){return <><Header/><main className="container simple-page"><p className="eyebrow">EXPLORE</p><h1>Categories</h1><p className="lead">Browse products by category and subcategory.</p><div className="category-list">{getCategories().map(c=><details key={c.name} className="category-item"><summary>{c.name}</summary>{c.subcategories.length>0&&<div className="subcats">{c.subcategories.map(s=><Link key={s} href={`/?q=${encodeURIComponent(s)}`}>{s}</Link>)}</div>}</details>)}</div></main><Footer/></>}
